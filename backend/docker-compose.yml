version: "3"
services:
  video-touch:
    build: .
    hostname: assets-touch
    image: video-touch
    container_name: assets-touch
    restart: unless-stopped
    env_file: .env
    environment:
      - PORT=$PORT
    ports:
      - $PORT:$PORT